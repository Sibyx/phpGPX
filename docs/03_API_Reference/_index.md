# API Reference

The API reference is auto-generated from PHPDoc comments by [phpDocumentor](https://www.phpdoc.org/).

Build the full documentation site (API reference + guides) with:

```bash
composer run docs:build
```

The output goes to `docs-output/`. API class pages are under `docs-output/classes/`.